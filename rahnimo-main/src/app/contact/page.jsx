import React from 'react';
import ContactClient from './ContactClient';

export const metadata = {
  title: "Contact",
  description: "This interior Studio",
};

const Contact = () => {
    return <ContactClient />
};

export default Contact;